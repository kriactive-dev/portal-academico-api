# Adicione estas variáveis ao seu arquivo .env

# ============================================
# FIREBASE CONFIGURATION
# ============================================

# ID do seu projeto Firebase (obrigatório)
FIREBASE_PROJECT_ID=seu-projeto-firebase

# Caminho para o arquivo de credenciais JSON (obrigatório)
# Baixe do Firebase Console > Configurações > Contas de serviço
FIREBASE_CREDENTIALS_PATH=storage/app/firebase/firebase-credentials.json

# URL do Realtime Database (opcional)
FIREBASE_DATABASE_URL=https://seu-projeto-default-rtdb.firebaseio.com/

# Bucket do Storage (opcional)
FIREBASE_STORAGE_BUCKET=seu-projeto.appspot.com

# Configurações de autenticação
FIREBASE_VERIFY_EMAIL=true
FIREBASE_AUTO_CREATE_USERS=true
FIREBASE_CACHE_TTL=3600

# ============================================
# INSTRUÇÕES DE CONFIGURAÇÃO
# ============================================

# 1. Acesse o Firebase Console: https://console.firebase.google.com
# 2. Selecione seu projeto
# 3. Vá em Configurações > Contas de serviço
# 4. Clique em "Gerar nova chave privada"
# 5. Baixe o arquivo JSON
# 6. Salve como storage/app/firebase/firebase-credentials.json
# 7. Configure as variáveis acima com os valores do seu projeto

# ============================================
# EXEMPLO DE USO NO FRONTEND
# ============================================

# JavaScript/React:
# const idToken = await user.getIdToken();
# fetch('/api/auth/firebase/login', {
#   method: 'POST',
#   headers: { 'Content-Type': 'application/json' },
#   body: JSON.stringify({ firebase_token: idToken })
# });